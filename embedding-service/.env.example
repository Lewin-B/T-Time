# T-Time Embedding Service Environment Variables
# This service doesn't require any environment variables by default
# but this file is here for future configuration needs

# Port (default: 8000)
# PORT=8000

# Log level (default: info)
# LOG_LEVEL=info
